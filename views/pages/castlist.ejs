<!DOCTYPE html>
<html>
<head>
  <% include ../partials/header.ejs %>
</head>

<body>

<% include ../partials/nav.ejs %>

<div class="container">
<h2>Cast List</h2>

<ul>
    <table border=1>
        <tr>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Movie ID</th>
        </tr>
        <% result.rows.forEach(function(row) { %>
            <tr>
                <td><%= row.fname %></td>
                <td><%= row.lname %></td>
                <td><%= row.movieid %></td>
            </tr>
        <% }); %>
    </table>

</ul>

</div>

</body>
</html>
